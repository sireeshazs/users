@Library('todo') _

todo (
        COMPONENT             : 'users',
        PROJECT_NAME          : "TODOAPP",
        SLAVE_LABEL           : "MASTER",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NGINX"